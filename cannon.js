class Cannon {
  constructor(x, y, w, h, angle) {
    //this.
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
    this.angle = angle;
  }

  show() {
    //base do canhão
    //topo do canhão
  }
}
